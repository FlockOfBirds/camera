// import { shallow } from "enzyme";
// import { createElement } from "react";

// import { CameraButton, CameraButtonProps } from "../CameraButton";

describe("Camera", () => {
    /// const createCameraButton = (props: CameraButtonProps) => shallow(createElement(CameraButton, props));

    it("should render the structure appropriately", () => {
        //
    });

    it("should not create button when default props are not provided", () => {
        //
    });

    it("should render an alert message when browser does not support the widget", () => {
        //
    });

    it("should create switch/swap camera button when there is more than one media device", () => {
        //
    });

    it("should render webcam when retake picture/camera button is clicked ", () => {
        //
    });

    it("should enumerate available media devices on before mounting", () => {
        //
    });

    it("should switch/swap camera when switch camera button is clicked", () => {
        //
    });

    it("should respond to onclick actions", () => {
        //
    });

    describe("When take picture/camera is clicked", () => {
        it("should stop all media video devices", () => {
            //
        });

        it("should display/render the picture", () => {
            //
        });
    });
});
